#include "main.h"
/**
* _abs -a function that returns the absolute value of a number.
* @num : checked parameter
* Return: 0 Success
*/
int _abs(int num)
{
if (num < 0)
{
return (-num);
}
return (num);
}
